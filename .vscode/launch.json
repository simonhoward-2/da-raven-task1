{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C#: LocationsAPI Debug",
            "type": "dotnet",
            "request": "launch",
            "preLaunchTask": "build",
            "projectPath": "${workspaceFolder}/src/LocationsAPI/LocationsAPI.csproj"
        }
    ]
}